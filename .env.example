# 백엔드 ngrok URL 설정 예시
# 백엔드를 ngrok으로 노출한 후 아래 URL들을 실제 ngrok URL로 변경하세요

# API 서버 URL (HTTP API 요청용)
REACT_APP_API_URL=https://your-backend-ngrok-url.ngrok.io

# WebSocket 서버 URL (실시간 통신용)  
REACT_APP_WS_URL=wss://your-backend-ngrok-url.ngrok.io

# 사용법:
# 1. 이 파일을 .env로 복사: cp .env.example .env
# 2. 백엔드 ngrok URL로 위 URL들을 수정
# 3. npm start로 프론트엔드 실행